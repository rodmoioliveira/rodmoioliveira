```terminal
> get name
> rudi
>
> [terminal] get name --verbose
> Rodolfo Moi de Oliveira
```

```bash
> get name
> rudi
>
> [terminal] get name --verbose
> Rodolfo Moi de Oliveira
> info which
> which -a
> @root which
@root which
@root: which
> help
```

```zsh
> get name
> rudi
>
[terminal] > get name --verbose
> Rodolfo Moi de Oliveira
> info which
> which -a
> @root which
@root which
@root: which
> help
```

```clj
(-> developer (get :name))
```
